name: Bot source diagnostic

on:
  push:
    branches:
      - test/classroom-smoke

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node scripts/inspect-bot-source.js
